const Joi = require('joi');

const ruleCreateOrUpdate = {

};

const validateRules = {
  createBorrowRequest: {
    body: ruleCreateOrUpdate,
  },
  createBorrowRequest: {
    body: ruleCreateOrUpdate,
  },
};

module.exports = validateRules;
