
const MEASURE = {
    PERCENT: 1, // Tỷ lệ
};

const CRITERIA = {
    REVENE: 1, // doanh thu
    PROFIT: 2, // loi nhuan
    NO_ORDERS: 3 // so luong don hang
};

const VALUE_TYPES = {
    PERCENT: 1, // %
    VND: 2, // đ
    NULL: 3 // null 
};

module.exports = {
    MEASURE,
    CRITERIA,
    VALUE_TYPES
};
